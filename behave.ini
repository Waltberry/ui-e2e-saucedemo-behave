[behave]
# default output; CI overrides to also produce JUnit
format = progress
# default tags (none); example: --tags ~@wip
# tags = 
# don't stop on first failure
stop = false
# show timing
show_timings = true
# junit directory (CI will set -f junit -o reports/junit)
junit_directory = reports/junit